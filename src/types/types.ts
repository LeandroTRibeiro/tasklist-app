export type ListType = {
    _id: string,
    title: string,
    description: string,
    done: boolean
};

export type PropsItem = {
    data: {
        _id: string,
        title: string,
        description: string,
        done: boolean
    }
}
